import { Col } from "react-bootstrap";

export default function CommentArea(){
    return(
        <Col className="mt-2">
        <input type="text" />
        <button>Leave a comment</button>
        </Col>
    )
}